[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mebalance"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "iapws",
    "openpyxl",
]

[tool.setuptools]
py-modules = ["SugarStream", "SteamStream", "Evaporator"]